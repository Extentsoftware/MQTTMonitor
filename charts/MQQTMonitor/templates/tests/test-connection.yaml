apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "MQQTMonitor.fullname" . }}-test-connection"
  labels:
    {{- include "MQQTMonitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "MQQTMonitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
